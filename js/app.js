const EMBEDDED_GEOJSON_DRAWINGS = {"type": "FeatureCollection", "features": [{"type": "Feature", "properties": {"Name": "MTL HALL 04", "description": null, "name": "MTL HALL 04", "category": "Academic & Lecture Halls", "id": "mtl_hall_04"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8718048, 6.9749907], [79.8718223, 6.9749015], [79.8718638, 6.9749108], [79.8718471, 6.9749984], [79.8718048, 6.9749907]]]}}, {"type": "Feature", "properties": {"Name": "UNION ROOM", "description": null, "name": "UNION ROOM", "category": "Facilities & Dining", "id": "union_room"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8717203, 6.9749385], [79.8717257, 6.9749039], [79.8717901, 6.9749093], [79.8717874, 6.9749465], [79.8717203, 6.9749385]]]}}, {"type": "Feature", "properties": {"Name": "SPORT ROOM", "description": null, "name": "SPORT ROOM", "category": "Sports & Rec", "id": "sport_room"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8717203, 6.9749385], [79.8717874, 6.9749465], [79.8717846, 6.9749958], [79.8717123, 6.9749891], [79.8717203, 6.9749385]]]}}, {"type": "Feature", "properties": {"Name": "MTL HALL 03", "description": null, "name": "MTL HALL 03", "category": "Academic & Lecture Halls", "id": "mtl_hall_03"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8721033, 6.9750549], [79.8721214, 6.9749711], [79.8721703, 6.9749797], [79.8721502, 6.9750629], [79.8721033, 6.9750549]]]}}, {"type": "Feature", "properties": {"Name": "LECTURERS WASHROOM", "description": null, "name": "LECTURERS WASHROOM", "category": "Amenities", "id": "lecturers_washroom"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8721502, 6.9750629], [79.8721703, 6.9749797], [79.8722106, 6.9749857], [79.8721919, 6.9750702], [79.8721502, 6.9750629]]]}}, {"type": "Feature", "properties": {"Name": "CLASS ROOM 07", "description": null, "name": "CLASS ROOM 07", "category": "Academic & Lecture Halls", "id": "class_room_07"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8721919, 6.9750702], [79.8722106, 6.9749857], [79.8722649, 6.974993], [79.8722455, 6.9750789], [79.8721919, 6.9750702]]]}}, {"type": "Feature", "properties": {"Name": "VOLLY BALL COURT", "description": null, "name": "VOLLY BALL COURT", "category": "Sports & Rec", "id": "volly_ball_court"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8722482, 6.9751095], [79.8722669, 6.9750256], [79.8724319, 6.9750523], [79.8724104, 6.9751361], [79.8722482, 6.9751095]]]}}, {"type": "Feature", "properties": {"Name": "BADMINTON COURT", "description": null, "name": "BADMINTON COURT", "category": "Sports & Rec", "id": "badminton_court"}, "geometry": {"type": "Polygon", "coordinates": [[[79.871933, 6.9751867], [79.8719585, 6.9750443], [79.8720336, 6.9750616], [79.8720041, 6.975196], [79.871933, 6.9751867]]]}}, {"type": "Feature", "properties": {"Name": "LECTUERE'S ROOM", "description": null, "name": "LECTUERE'S ROOM", "category": "Academic & Lecture Halls", "id": "lectueres_room"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8718471, 6.9749984], [79.8718638, 6.9749108], [79.8720759, 6.9749524], [79.8720577, 6.975039], [79.8718471, 6.9749984]]]}}, {"type": "Feature", "properties": {"Name": "TRAINING AND ACCOUNT DIVISION", "description": null, "name": "TRAINING AND ACCOUNT DIVISION", "category": "Administrative", "id": "training_and_account_division"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8716869, 6.975371], [79.8717127, 6.975192], [79.8717563, 6.9751987], [79.871815, 6.9752083], [79.8717761, 6.9753837], [79.8716869, 6.975371]]]}}, {"type": "Feature", "properties": {"Name": "AUDITORIUM", "description": null, "name": "AUDITORIUM", "category": "Labs & Workshops", "id": "auditorium"}, "geometry": {"type": "Polygon", "coordinates": [[[79.871815, 6.9752083], [79.871927, 6.975232], [79.8718955, 6.9754057], [79.8717761, 6.9753837], [79.871815, 6.9752083]]]}}, {"type": "Feature", "properties": {"Name": "MTL HALL 02", "description": null, "name": "MTL HALL 02", "category": "Academic & Lecture Halls", "id": "mtl_hall_02"}, "geometry": {"type": "Polygon", "coordinates": [[[79.871937, 6.9753225], [79.8719538, 6.9752346], [79.8720128, 6.9752426], [79.8719987, 6.9753358], [79.871937, 6.9753225]]]}}, {"type": "Feature", "properties": {"Name": "STORAGE ROOM", "description": null, "name": "STORAGE ROOM", "category": "Amenities", "id": "storage_room"}, "geometry": {"type": "Polygon", "coordinates": [[[79.871937, 6.9753225], [79.8719116, 6.9753185], [79.871927, 6.975232], [79.8719538, 6.9752346], [79.871937, 6.9753225]]]}}, {"type": "Feature", "properties": {"Name": "MTL HALL 01", "description": null, "name": "MTL HALL 01", "category": "Academic & Lecture Halls", "id": "mtl_hall_01"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8724178, 6.9752586], [79.8724399, 6.9751514], [79.8725606, 6.9751727], [79.8725372, 6.9752832], [79.8724178, 6.9752586]]]}}, {"type": "Feature", "properties": {"Name": "CLASS ROOM 02", "description": null, "name": "CLASS ROOM 02", "category": "Academic & Lecture Halls", "id": "class_room_02"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8724178, 6.9752586], [79.8723437, 6.9752476], [79.8723642, 6.9751627], [79.8724366, 6.9751761], [79.8724178, 6.9752586]]]}}, {"type": "Feature", "properties": {"Name": "CLASS ROOM 03", "description": null, "name": "CLASS ROOM 03", "category": "Academic & Lecture Halls", "id": "class_room_03"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8723642, 6.9751627], [79.8723437, 6.9752476], [79.8722878, 6.9752393], [79.8723078, 6.9751521], [79.8723642, 6.9751627]]]}}, {"type": "Feature", "properties": {"Name": "DRAWING ROOM", "description": null, "name": "DRAWING ROOM", "category": "Academic & Lecture Halls", "id": "drawing_room"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8722878, 6.9752393], [79.8721657, 6.9752187], [79.8721845, 6.9751361], [79.8723078, 6.9751521], [79.8722878, 6.9752393]]]}}, {"type": "Feature", "properties": {"Name": "CLASS ROOM 04", "description": null, "name": "CLASS ROOM 04", "category": "Academic & Lecture Halls", "id": "class_room_04"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8721657, 6.9752187], [79.8720866, 6.975208], [79.872104, 6.9751241], [79.8721845, 6.9751361], [79.8721657, 6.9752187]]]}}, {"type": "Feature", "properties": {"Name": "STUDENTS WASH ROOM", "description": null, "name": "STUDENTS WASH ROOM", "category": "Amenities", "id": "students_wash_room"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8720946, 6.9752346], [79.8721543, 6.9752379], [79.8721496, 6.9752599], [79.8720946, 6.9752546], [79.8720946, 6.9752346]]]}}, {"type": "Feature", "properties": {"Name": "TRANSPORT DIVISION", "description": null, "name": "TRANSPORT DIVISION", "category": "Administrative", "id": "transport_division"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8720946, 6.9752546], [79.8721496, 6.9752599], [79.8721483, 6.9752892], [79.8720919, 6.9752839], [79.8720946, 6.9752546]]]}}, {"type": "Feature", "properties": {"Name": "IT LAB", "description": null, "name": "IT LAB", "category": "Labs & Workshops", "id": "it_lab"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8720919, 6.9752839], [79.8721483, 6.9752892], [79.8721402, 6.9753424], [79.8720825, 6.9753358], [79.8720919, 6.9752839]]]}}, {"type": "Feature", "properties": {"Name": "CHART ROOM", "description": null, "name": "CHART ROOM", "category": "Labs & Workshops", "id": "chart_room"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8720825, 6.9753358], [79.8721402, 6.9753424], [79.8721281, 6.9754186], [79.8720705, 6.9754146], [79.8720825, 6.9753358]]]}}, {"type": "Feature", "properties": {"Name": "WORKSHOP", "description": null, "name": "WORKSHOP", "category": "Labs & Workshops", "id": "workshop"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8717583, 6.9754104], [79.8722571, 6.9754982], [79.872229, 6.975622], [79.8717287, 6.9755328], [79.8717583, 6.9754104]]]}}, {"type": "Feature", "properties": {"Name": "WELDING WORKSHOP", "description": null, "name": "WELDING WORKSHOP", "category": "Labs & Workshops", "id": "welding_workshop"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8721016, 6.9756104], [79.8722183, 6.975629], [79.8722035, 6.9757129], [79.8720882, 6.9756929], [79.8721016, 6.9756104]]]}}, {"type": "Feature", "properties": {"Name": "CANTEEN", "description": null, "name": "CANTEEN", "category": "Facilities & Dining", "id": "canteen"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8716057, 6.9755854], [79.8717077, 6.9756001], [79.8716822, 6.9757545], [79.8715762, 6.9757412], [79.8716057, 6.9755854]]]}}, {"type": "Feature", "properties": {"Name": "GYM", "description": null, "name": "GYM", "category": "Sports & Rec", "id": "gym"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8715561, 6.975857], [79.8715682, 6.9757824], [79.8716781, 6.9758011], [79.8716634, 6.9758716], [79.8715561, 6.975857]]]}}, {"type": "Feature", "properties": {"Name": "BOYS HOSTALS", "description": null, "name": "BOYS HOSTALS", "category": "Facilities & Dining", "id": "boys_hostals"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8716634, 6.9758716], [79.8717077, 6.9756001], [79.8719984, 6.9756537], [79.8719489, 6.9759228], [79.8716634, 6.9758716]]]}}, {"type": "Feature", "properties": {"Name": "REGIONAL CENTER MATTAKKULIYA", "description": null, "name": "REGIONAL CENTER MATTAKKULIYA", "category": "Administrative", "id": "regional_center_mattakkuliya"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8717089, 6.9760632], [79.8717331, 6.975962], [79.8719691, 6.9760046], [79.8719503, 6.9761058], [79.8717089, 6.9760632]]]}}, {"type": "Feature", "properties": {"Name": "CLASS ROOM 08", "description": null, "name": "CLASS ROOM 08", "category": "Academic & Lecture Halls", "id": "class_room_08"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8722234, 6.9749549], [79.8722476, 6.9748804], [79.8723495, 6.974903], [79.8723334, 6.9749735], [79.8722234, 6.9749549]]]}}, {"type": "Feature", "properties": {"Name": "LAB", "description": null, "name": "LAB", "category": "Labs & Workshops", "id": "lab"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8722234, 6.9749549], [79.8720377, 6.9749243], [79.8720498, 6.9748484], [79.8722476, 6.9748804], [79.8722234, 6.9749549]]]}}, {"type": "Feature", "properties": {"Name": "classrooms", "description": null, "name": "classrooms", "category": "Academic & Lecture Halls", "id": "classrooms"}, "geometry": {"type": "Polygon", "coordinates": [[[79.871815, 6.9752083], [79.8718275, 6.9750235], [79.8719334, 6.9750395], [79.8719173, 6.9752312], [79.871815, 6.9752083]]]}}, {"type": "Feature", "properties": {"Name": "MTL HALL 04", "description": "MTL LECTURE HALL 04", "name": "MTL HALL 04", "category": "Academic & Lecture Halls", "id": "mtl_hall_04"}, "geometry": {"type": "Point", "coordinates": [79.8718357, 6.9749505, 0.0]}}, {"type": "Feature", "properties": {"Name": "Security Room", "description": null, "name": "Security Room", "category": "Security & Entry", "id": "security_room"}, "geometry": {"type": "Polygon", "coordinates": [[[79.8717728, 6.9747794], [79.8718103, 6.9747528], [79.8718331, 6.9747741], [79.8717942, 6.974802], [79.8717728, 6.9747794]]]}}, {"type": "Feature", "properties": {"Name": "Sport Room", "name": "Sport Room", "category": "Sports & Rec", "id": "sport_room"}, "geometry": {"type": "Point", "coordinates": [79.8717309, 6.9749695, 0.0]}}, {"type": "Feature", "properties": {"Name": "UNION ROOM", "name": "UNION ROOM", "category": "Facilities & Dining", "id": "union_room"}, "geometry": {"type": "Point", "coordinates": [79.8717243, 6.9749225, 0.0]}}]};
// Main Campus Navigation Application with QR Scan Welcome Modal & Zero Clipping Router
document.addEventListener('DOMContentLoaded', async () => {
  let map = null;
  let campusData = null;
  let rawGeoJSON = null;
  let router = null;
  
  // Navigation & Location State
  let currentStartLoc = null; // null = use live GPS position
  let currentDestLoc = null;  
  let userLivePos = null;     // { lat, lon, accuracy }
  let watchPositionId = null;

  // Map Layers
  let activeRoutePolyline = null;
  let activeRouteOuterGlow = null;
  let activeRouteMarkers = [];
  let userLiveMarker = null;
  let userAccuracyCircle = null;
  let geojsonLayer = null;
  let tileLayers = {};
  let activeCategory = 'all';

  // DOM Elements
  const searchInput = document.getElementById('search-input');
  const clearSearchBtn = document.getElementById('clear-search-btn');
  const categoryChips = document.getElementById('category-chips');
  const locationListView = document.getElementById('location-list-view');
  const routeNavView = document.getElementById('route-nav-view');
  const routeDistanceEl = document.getElementById('route-distance');
  const routeTimeEl = document.getElementById('route-time');
  const turnStepsList = document.getElementById('turn-steps-list');
  const clearRouteBtn = document.getElementById('clear-route-btn');
  const accessibleToggle = document.getElementById('accessible-toggle');
  const qrBanner = document.getElementById('qr-welcome-banner');
  const qrBannerText = document.getElementById('qr-welcome-text');
  const langSelect = document.getElementById('lang-select');

  // Welcome Modal Elements
  const welcomeModal = document.getElementById('welcome-modal');
  const welcomeDestSelect = document.getElementById('welcome-dest-select');
  const gpsStatusBox = document.getElementById('gps-status-box');
  const startWelcomeNavBtn = document.getElementById('start-welcome-nav-btn');

  // Modals
  const floorModal = document.getElementById('floor-plan-modal');
  const closeFloorModalBtn = document.getElementById('close-floor-modal');
  const modalBuildingTitle = document.getElementById('modal-building-title');
  const floorTabsContainer = document.getElementById('floor-tabs');
  const indoorRoomsGrid = document.getElementById('indoor-rooms-grid');

  const adminModal = document.getElementById('admin-modal');
  const openAdminBtn = document.getElementById('open-admin-btn');
  const closeAdminModalBtn = document.getElementById('close-admin-modal');
  const adminPasscode = document.getElementById('admin-passcode');
  const adminLoginBtn = document.getElementById('admin-login-btn');

  // Initialize Application
  async function init() {
    initMap();
    if (window.FALLBACK_CAMPUS_DATA) {
      campusData = window.FALLBACK_CAMPUS_DATA;
      if (campusData.graph) {
        router = new CampusRouter(campusData.graph.nodes, campusData.graph.edges);
      }
      populateRoutePickers();
    }
    if (window.FALLBACK_RAW_GEOJSON) {
      rawGeoJSON = window.FALLBACK_RAW_GEOJSON;
      renderGeoJSONLayer();
    }
    setupLanguage();
    setupEventListeners();
    initLiveGeolocation();
    checkURLParams();
    renderLocationList();

    // Async background refresh if online
    loadData().catch(e => console.log('Loaded from embedded dataset:', e));
  }

  // 1. Initialize Map with Google Maps Tiles & Strict University Bounds Lock
  function initMap() {
    const campusBounds = L.latLngBounds(
      [6.9735, 79.8700], // South-West corner
      [6.9768, 79.8735]  // North-East corner
    );

    map = L.map('map', {
      center: [6.975235, 79.872020],
      zoom: 18,
      minZoom: 16,
      maxZoom: 21,
      maxBounds: campusBounds,
      maxBoundsViscosity: 1.0,
      zoomControl: false
    });

    L.control.zoom({ position: 'topright' }).addTo(map);

    // Google Maps Tile Layers Integration
    tileLayers['Google Streets'] = L.tileLayer('https://{s}.google.com/vt/lyrs=m&x={x}&y={y}&z={z}', {
      maxZoom: 21,
      subdomains: ['mt0', 'mt1', 'mt2', 'mt3'],
      attribution: '&copy; Google Maps'
    });

    tileLayers['Google Hybrid Satellite'] = L.tileLayer('https://{s}.google.com/vt/lyrs=s,h&x={x}&y={y}&z={z}', {
      maxZoom: 21,
      subdomains: ['mt0', 'mt1', 'mt2', 'mt3'],
      attribution: '&copy; Google Maps Satellite'
    });

    tileLayers['Positron Clean'] = L.tileLayer('https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png', {
      attribution: '&copy; OpenStreetMap &copy; CARTO',
      subdomains: 'abcd',
      maxZoom: 20
    });

    tileLayers['Google Streets'].addTo(map);

    // Immediately render embedded GeoJSON building polygon drawings
    renderGeoJSONLayer();

    setTimeout(() => {
      if (map) map.invalidateSize();
    }, 200);

    // Quick Map Action Control Handlers
    const zoomInBtn = document.getElementById('btn-zoom-in');
    if (zoomInBtn) zoomInBtn.addEventListener('click', () => map.zoomIn());

    const zoomOutBtn = document.getElementById('btn-zoom-out');
    if (zoomOutBtn) zoomOutBtn.addEventListener('click', () => map.zoomOut());

    const centerCampusBtn = document.getElementById('btn-recenter-campus');
    if (centerCampusBtn) {
      centerCampusBtn.addEventListener('click', () => {
        if (geojsonLayer && geojsonLayer.getBounds().isValid()) {
          map.flyToBounds(geojsonLayer.getBounds(), { duration: 0.6, padding: [40, 40] });
        } else {
          map.flyToBounds(campusBounds, { duration: 0.6 });
        }
      });
    }

    const recenterGpsBtn = document.getElementById('btn-recenter-user') || document.getElementById('btn-recenter-gps');
    if (recenterGpsBtn) {
      recenterGpsBtn.addEventListener('click', () => {
        if (userLivePos) {
          map.flyTo([userLivePos.lat, userLivePos.lon], 19, { duration: 0.6 });
        } else {
          navigator.geolocation.getCurrentPosition(
            (pos) => {
              updateUserLivePosition(pos.coords.latitude, pos.coords.longitude, pos.coords.accuracy, true);
              map.flyTo([pos.coords.latitude, pos.coords.longitude], 19, { duration: 0.6 });
            },
            () => alert('Please enable GPS location permissions on your phone.'),
            { enableHighAccuracy: true }
          );
        }
      });
    }

    // Map Tile Switcher Listeners
    const layerToggleBtn = document.getElementById('btn-layer-toggle');
    const layerMenu = document.getElementById('layer-menu');
    if (layerToggleBtn && layerMenu) {
      layerToggleBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        layerMenu.classList.toggle('active');
      });
      document.addEventListener('click', () => layerMenu.classList.remove('active'));
    }

    document.querySelectorAll('.layer-option').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const layerKey = btn.getAttribute('data-layer');
        if (tileLayers[layerKey]) {
          Object.keys(tileLayers).forEach(k => map.removeLayer(tileLayers[k]));
          tileLayers[layerKey].addTo(map);
          document.querySelectorAll('.layer-option').forEach(b => b.classList.remove('active'));
          btn.classList.add('active');
          if (layerMenu) layerMenu.classList.remove('active');
        }
      });
    });

    // Google Maps Style Directions FAB Listener
    const gmapsFabBtn = document.getElementById('btn-gmaps-directions');
    if (gmapsFabBtn) {
      gmapsFabBtn.addEventListener('click', () => {
        locationListView.style.display = 'none';
        routeNavView.style.display = 'flex';
        const destSelect = document.getElementById('dest-select');
        if (destSelect && destSelect.options.length > 1) {
          if (!destSelect.value) destSelect.selectedIndex = 1;
          const loc = campusData.locations.find(l => l.id === destSelect.value);
          if (loc) startRouteNavigation(loc);
        }
      });
    }
  }

  // 2. Load Data with Instant Failsafe Fallback
  async function loadData() {
    try {
      if (window.FALLBACK_CAMPUS_DATA) {
        campusData = window.FALLBACK_CAMPUS_DATA;
        populateRoutePickers();
      }
      if (window.FALLBACK_RAW_GEOJSON) {
        rawGeoJSON = window.FALLBACK_RAW_GEOJSON;
        renderGeoJSONLayer();
      }

      const ts = Date.now();
      const res = await fetch(`data/campus_data.json?v=${ts}`);
      if (res.ok) {
        campusData = await res.json();
        populateRoutePickers();
      }

      const geoRes = await fetch(`data/Drawing.geojson?v=${ts}`);
      if (geoRes.ok) {
        rawGeoJSON = await geoRes.json();
        renderGeoJSONLayer();
      }

      if (campusData && campusData.graph) {
        router = new CampusRouter(campusData.graph.nodes, campusData.graph.edges);
      }
      await CMS.init();
    } catch (e) {
      console.warn('Using offline failsafe dataset:', e);
      if (window.FALLBACK_CAMPUS_DATA) {
        campusData = window.FALLBACK_CAMPUS_DATA;
        populateRoutePickers();
      }
      if (window.FALLBACK_RAW_GEOJSON) {
        rawGeoJSON = window.FALLBACK_RAW_GEOJSON;
        renderGeoJSONLayer();
      }
    }
  }

  // Populate Dropdowns & Welcome Modal Selection
  function populateRoutePickers() {
    const originSelect = document.getElementById('origin-select');
    const destSelect = document.getElementById('dest-select');
    const welcomeDestSelect = document.getElementById('welcome-dest-select');

    if (!campusData || !campusData.locations) return;

    if (originSelect) originSelect.innerHTML = `<option value="live">📍 My Actual Live GPS Position</option>`;
    if (destSelect) destSelect.innerHTML = `<option value="">🎯 Choose Destination...</option>`;
    if (welcomeDestSelect) welcomeDestSelect.innerHTML = `<option value="">-- Choose Landmark / Classroom --</option>`;

    campusData.locations.forEach(loc => {
      const transName = loc.translations ? (loc.translations[i18n.currentLang] || loc.name) : loc.name;
      if (originSelect) originSelect.innerHTML += `<option value="${loc.id}">${transName}</option>`;
      if (destSelect) destSelect.innerHTML += `<option value="${loc.id}">${transName}</option>`;
      if (welcomeDestSelect) welcomeDestSelect.innerHTML += `<option value="${loc.id}">${transName}</option>`;
    });

    const defaultStart = campusData.locations.find(l => l.id === 'security_room') || campusData.locations[0];
    if (originSelect && defaultStart) originSelect.value = defaultStart.id;
    currentStartLoc = defaultStart;

    renderLocationList();

    originSelect.addEventListener('change', (e) => {
      const val = e.target.value;
      if (val === 'live') {
        currentStartLoc = null;
      } else {
        currentStartLoc = campusData.locations.find(l => l.id === val);
      }
      if (currentDestLoc) recalculateLiveRoute();
    });

    destSelect.addEventListener('change', (e) => {
      const val = e.target.value;
      if (!val) {
        clearRoute();
      } else {
        const loc = campusData.locations.find(l => l.id === val);
        if (loc) startRouteNavigation(loc);
      }
    });

    // Swap Origin & Destination
    document.getElementById('swap-route-btn').addEventListener('click', () => {
      const origVal = originSelect.value;
      const destVal = destSelect.value;

      if (!destVal) {
        alert('Please select a destination first to swap!');
        return;
      }

      if (origVal === 'live') {
        originSelect.value = destVal;
        destSelect.value = 'security_room';
        currentStartLoc = campusData.locations.find(l => l.id === destVal);
        const newDest = campusData.locations.find(l => l.id === 'security_room');
        if (newDest) startRouteNavigation(newDest);
      } else {
        originSelect.value = destVal;
        destSelect.value = origVal;
        currentStartLoc = campusData.locations.find(l => l.id === destVal);
        const newDest = campusData.locations.find(l => l.id === origVal);
        if (newDest) startRouteNavigation(newDest);
      }
    });

    // Welcome Modal Action - Start Precise Navigation Button Handler
    startWelcomeNavBtn.addEventListener('click', () => {
      let selectedId = welcomeDestSelect.value;
      
      if (!selectedId) {
        welcomeDestSelect.style.border = '2px solid #ef4444';
        welcomeDestSelect.style.boxShadow = '0 0 16px rgba(239, 68, 68, 0.6)';
        welcomeDestSelect.focus();
        return;
      }

      welcomeDestSelect.style.border = '1px solid var(--primary-500)';
      welcomeDestSelect.style.boxShadow = 'none';

      const targetLoc = campusData.locations.find(l => l.id === selectedId);
      if (targetLoc) {
        welcomeModal.classList.remove('active');
        welcomeModal.style.display = 'none';
        startRouteNavigation(targetLoc);
      }
    });

    const closeWelcomeModalBtn = document.getElementById('close-welcome-modal-btn');
    if (closeWelcomeModalBtn) {
      closeWelcomeModalBtn.addEventListener('click', () => {
        welcomeModal.classList.remove('active');
        welcomeModal.style.display = 'none';
      });
    }

    // Welcome Modal Action - Take Own Navigation Button Handler
    document.getElementById('welcome-custom-pin-btn').addEventListener('click', () => {
      welcomeModal.classList.remove('active');
      welcomeModal.style.display = 'none';
      alert('📍 Custom Navigation Mode Active:\n\nTap ANY location on the Ocean University map to set your custom start point or destination!');
    });

    // Reset border styling on select change
    welcomeDestSelect.addEventListener('change', () => {
      if (welcomeDestSelect.value) {
        welcomeDestSelect.style.border = '1px solid var(--primary-500)';
        welcomeDestSelect.style.boxShadow = 'none';
      }
    });
  }

  // 3. Render Vector Polygon Layers & Permanent Building Markers on Map
  function renderGeoJSONLayer() {
    if (!map) return;

    function getCategoryColor(name) {
      if (name.includes('SECURITY') || name.includes('REGIONAL')) return '#ef4444';
      if (name.includes('MTL') || name.includes('CLASS') || name.includes('DRAWING')) return '#6366f1';
      if (name.includes('LAB') || name.includes('WORKSHOP')) return '#06b6d4';
      if (name.includes('DIVISION')) return '#a855f7';
      if (name.includes('SPORT') || name.includes('GYM') || name.includes('COURT')) return '#10b981';
      return '#f59e0b';
    }

    if (geojsonLayer) map.removeLayer(geojsonLayer);
    bldgMarkers.forEach(m => map.removeLayer(m));
    bldgMarkers = [];

    const targetGeoJSON = rawGeoJSON || EMBEDDED_GEOJSON_DRAWINGS;
    if (targetGeoJSON) {
      geojsonLayer = L.geoJSON(targetGeoJSON, {
        style: (feature) => {
          const name = feature.properties.name || '';
          const color = getCategoryColor(name.toUpperCase());
          return {
            color: '#0f172a',
            weight: 3.5,
            opacity: 1.0,
            fillColor: color,
            fillOpacity: 0.65
          };
        },
        onEachFeature: (feature, layer) => {
          const name = feature.properties.name || 'Campus Building';
          const loc = campusData ? campusData.locations.find(l => l.name.toUpperCase() === name.toUpperCase() || name.toUpperCase().includes(l.name.toUpperCase()) || l.name.toUpperCase().includes(name.toUpperCase())) : null;
          const transName = loc ? (loc.translations[i18n.currentLang] || loc.name) : name;
          const category = loc ? loc.category : 'Building';

          layer.bindTooltip(`<b>${transName}</b>`, {
            permanent: true,
            direction: 'center',
            className: 'bldg-tooltip-permanent'
          });

          const popupContent = `
            <div style="font-family: var(--font-body); padding: 4px; min-width: 200px;">
              <span style="font-size: 0.7rem; font-weight:700; color:${getCategoryColor(name.toUpperCase())}; text-transform:uppercase;">${category}</span>
              <h4 style="margin: 4px 0 2px 0; font-size: 1rem; color: #0f172a;">${transName}</h4>
              <p style="font-size: 0.72rem; color: #64748b; margin-bottom: 8px;"><i class="fa-solid fa-door-open"></i> Entrance Door</p>
              <div style="display:flex; gap:6px; flex-direction:column;">
                <button onclick="window.appSetStartLocation('${loc ? loc.id : ''}')" style="background:#0284c7; color:white; border:none; padding:7px 12px; border-radius:6px; font-weight:600; cursor:pointer; font-size:0.78rem;">
                  <i class="fa-solid fa-circle-dot"></i> Set as Start Location
                </button>
                <button onclick="window.appNavigateTo('${loc ? loc.id : ''}')" style="background:#4f46e5; color:white; border:none; padding:8px 12px; border-radius:6px; font-weight:600; cursor:pointer; font-size:0.8rem;">
                  <i class="fa-solid fa-diamond-turn-right"></i> ${i18n.t('navigateHere')}
                </button>
              </div>
            </div>
          `;
          layer.bindPopup(popupContent);
          layer.on('click', () => {
            if (loc) window.openBuildingInfoModal(loc);
          });
        }
      }).addTo(map);

      try {
        if (geojsonLayer.getBounds().isValid()) {
          map.fitBounds(geojsonLayer.getBounds(), { padding: [40, 40] });
        }
      } catch (e) {
        console.log('fitBounds info:', e);
      }
    }

    // Always render prominent markers for building locations
    if (campusData && campusData.locations) {
      campusData.locations.forEach(loc => {
        const color = getCategoryColor(loc.name.toUpperCase());
        const transName = loc.translations[i18n.currentLang] || loc.name;

        const bldgMarker = L.circleMarker([loc.lat, loc.lon], {
          radius: 6,
          fillColor: color,
          color: '#ffffff',
          weight: 2,
          fillOpacity: 1
        }).addTo(map);
        bldgMarkers.push(bldgMarker);

        bldgMarker.bindTooltip(`<b>${transName}</b>`, {
          permanent: false,
          direction: 'top',
          className: 'bldg-tooltip'
        });

        bldgMarker.on('click', () => {
          window.openBuildingInfoModal(loc);
        });

        const popupContent = `
          <div style="font-family: var(--font-body); padding: 4px; min-width: 200px;">
            <span style="font-size: 0.7rem; font-weight:700; color:${color}; text-transform:uppercase;">${loc.category}</span>
            <h4 style="margin: 4px 0 2px 0; font-size: 1rem; color: #0f172a;">${transName}</h4>
            <p style="font-size: 0.72rem; color: #64748b; margin-bottom: 8px;"><i class="fa-solid fa-door-open"></i> Entrance Door</p>
            <div style="display:flex; gap:6px; flex-direction:column;">
              <button onclick="window.appSetStartLocation('${loc.id}')" style="background:#0284c7; color:white; border:none; padding:7px 12px; border-radius:6px; font-weight:600; cursor:pointer; font-size:0.78rem;">
                <i class="fa-solid fa-circle-dot"></i> Set as Start Location
              </button>
              <button onclick="window.appNavigateTo('${loc.id}')" style="background:#4f46e5; color:white; border:none; padding:8px 12px; border-radius:6px; font-weight:600; cursor:pointer; font-size:0.8rem;">
                <i class="fa-solid fa-diamond-turn-right"></i> ${i18n.t('navigateHere')}
              </button>
            </div>
          </div>
        `;
        bldgMarker.bindPopup(popupContent);
      });
    }
  }

  // 4. Real-Time High-Accuracy Geolocation Tracking Engine
  function initLiveGeolocation() {
    if ('geolocation' in navigator) {
      navigator.geolocation.getCurrentPosition(
        (pos) => {
          updateUserLivePosition(pos.coords.latitude, pos.coords.longitude, pos.coords.accuracy, false);
        },
        (err) => {
          console.log('Initial geolocation prompt deferred:', err.message);
          if (gpsStatusBox) {
            gpsStatusBox.innerHTML = `<i class="fa-solid fa-triangle-exclamation" style="color:#f59e0b;"></i> Live GPS Signal Pending. Enable device location permissions.`;
          }
        },
        { enableHighAccuracy: true, timeout: 10000, maximumAge: 0 }
      );

      if (watchPositionId) navigator.geolocation.clearWatch(watchPositionId);

      watchPositionId = navigator.geolocation.watchPosition(
        (pos) => {
          const lat = pos.coords.latitude;
          const lon = pos.coords.longitude;
          const accuracy = pos.coords.accuracy;
          
          updateUserLivePosition(lat, lon, accuracy, false);
        },
        (err) => {
          console.warn('Geolocation watch notice:', err.message);
        },
        { enableHighAccuracy: true, maximumAge: 0, timeout: 15000 }
      );
    }
  }

  function updateUserLivePosition(lat, lon, accuracy = 3, isManualSim = false) {
    userLivePos = { lat, lon, accuracy };

    if (gpsStatusBox) {
      gpsStatusBox.innerHTML = `<i class="fa-solid fa-circle-check" style="color:var(--accent-emerald);"></i> 📍 Actual Live GPS Position Acquired (±${Math.round(accuracy)}m)`;
    }

    if (!userLiveMarker) {
      const liveIcon = L.divIcon({
        className: 'user-live-marker',
        iconSize: [20, 20],
        iconAnchor: [10, 10]
      });
      userLiveMarker = L.marker([lat, lon], { icon: liveIcon, zIndexOffset: 1000 }).addTo(map);
      userLiveMarker.bindTooltip("📍 Your Actual Live Position", { permanent: false, direction: 'top' });

      userAccuracyCircle = L.circle([lat, lon], {
        radius: accuracy,
        color: '#3b82f6',
        fillColor: '#3b82f6',
        fillOpacity: 0.18,
        weight: 1.5
      }).addTo(map);
    } else {
      userLiveMarker.setLatLng([lat, lon]);
      userAccuracyCircle.setLatLng([lat, lon]);
      userAccuracyCircle.setRadius(accuracy);
    }

    if (currentDestLoc) {
      recalculateLiveRoute();
    }
  }

  // 5. Recalculate Route between ANY Start Location & Destination
  function recalculateLiveRoute() {
    if (!router || !currentDestLoc) return;

    let routeResult = null;
    const isAccessibleOnly = accessibleToggle.checked;

    if (!currentStartLoc) {
      if (userLivePos) {
        routeResult = router.findShortestPathFromLocation(userLivePos.lat, userLivePos.lon, currentDestLoc.id, isAccessibleOnly);
      } else {
        const fallbackStart = campusData.locations.find(l => l.id === 'security_room') || campusData.locations[0];
        routeResult = router.findShortestPath(fallbackStart.id, currentDestLoc.id, isAccessibleOnly);
      }
    } else {
      routeResult = router.findShortestPath(currentStartLoc.id, currentDestLoc.id, isAccessibleOnly);
    }

    if (!routeResult) return;

    if (map) {
      setTimeout(() => {
        map.invalidateSize();
        renderGeoJSONLayer();
      }, 50);
    }

    locationListView.style.display = 'none';
    routeNavView.style.display = 'flex';

    routeDistanceEl.textContent = `${routeResult.totalDistance} m`;
    routeTimeEl.textContent = routeResult.timeFormatted;

    turnStepsList.innerHTML = '';

    const startCoordStr = currentStartLoc ? `${currentStartLoc.lat},${currentStartLoc.lon}` : userLivePos ? `${userLivePos.lat},${userLivePos.lon}` : '6.97475,79.87170';
    const gmapsUrl = `https://www.google.com/maps/dir/?api=1&origin=${startCoordStr}&destination=${currentDestLoc.lat},${currentDestLoc.lon}&travelmode=walking`;

    const gmapsBar = document.createElement('div');
    gmapsBar.style.padding = '6px 0';
    gmapsBar.style.marginBottom = '10px';
    gmapsBar.innerHTML = `
      <a href="${gmapsUrl}" target="_blank" style="display:flex; align-items:center; justify-content:center; gap:8px; background:#4285F4; color:white; padding:10px 16px; border-radius:8px; font-weight:700; text-decoration:none; font-size:0.85rem; box-shadow:0 4px 12px rgba(66,133,244,0.3);">
        <i class="fa-solid fa-map-location-dot"></i> Open Turn-by-Turn in Google Maps
      </a>
    `;
    turnStepsList.appendChild(gmapsBar);

    routeResult.steps.forEach(step => {
      const item = document.createElement('div');
      item.className = 'turn-step-item';
      item.innerHTML = `
        <div class="step-icon">${step.stepNum}</div>
        <div class="step-content">
          <p>${step.instruction}</p>
          <span>${step.distanceMeters} meters</span>
        </div>
      `;
      turnStepsList.appendChild(item);
    });

    if (activeRoutePolyline) map.removeLayer(activeRoutePolyline);
    if (activeRouteOuterGlow) map.removeLayer(activeRouteOuterGlow);
    activeRouteMarkers.forEach(m => map.removeLayer(m));
    activeRouteMarkers = [];

    activeRouteOuterGlow = L.polyline(routeResult.coordinates, {
      color: '#6366f1',
      weight: 10,
      opacity: 0.35,
      lineCap: 'round',
      lineJoin: 'round'
    }).addTo(map);

    activeRoutePolyline = L.polyline(routeResult.coordinates, {
      color: '#06b6d4',
      weight: 5,
      opacity: 0.95,
      dashArray: '8, 8',
      lineCap: 'round',
      lineJoin: 'round'
    }).addTo(map);

    routeResult.coordinates.forEach((coord, idx) => {
      if (idx > 0 && idx < routeResult.coordinates.length - 1) {
        const dot = L.circleMarker(coord, {
          radius: 4,
          fillColor: '#06b6d4',
          color: '#ffffff',
          weight: 1.5,
          fillOpacity: 0.9
        }).addTo(map);
        activeRouteMarkers.push(dot);
      }
    });

    if (currentStartLoc) {
      const startMarker = L.circleMarker([currentStartLoc.lat, currentStartLoc.lon], {
        radius: 8,
        fillColor: '#3b82f6',
        color: '#ffffff',
        weight: 2.5,
        fillOpacity: 1
      }).addTo(map).bindTooltip(`🚩 Start: ${currentStartLoc.name}`, { permanent: true, direction: 'bottom' });
      activeRouteMarkers.push(startMarker);
    }

    const goalMarker = L.circleMarker([currentDestLoc.lat, currentDestLoc.lon], {
      radius: 9,
      fillColor: '#10b981',
      color: '#ffffff',
      weight: 3,
      fillOpacity: 1
    }).addTo(map).bindTooltip(`🎯 Entrance Door: ${currentDestLoc.name}`, { permanent: true, direction: 'top' });

    activeRouteMarkers.push(goalMarker);
  }

  function startRouteNavigation(destLoc) {
    currentDestLoc = destLoc;
    const destSelect = document.getElementById('dest-select');
    if (destSelect) destSelect.value = destLoc.id;

    recalculateLiveRoute();

    if (activeRouteOuterGlow) {
      map.flyToBounds(activeRouteOuterGlow.getBounds(), { padding: [60, 60], duration: 0.6 });
    }
  }

  function clearRoute() {
    currentDestLoc = null;
    if (activeRoutePolyline) map.removeLayer(activeRoutePolyline);
    if (activeRouteOuterGlow) map.removeLayer(activeRouteOuterGlow);
    activeRouteMarkers.forEach(m => map.removeLayer(m));
    activeRouteMarkers = [];

    const destSelect = document.getElementById('dest-select');
    if (destSelect) destSelect.value = '';

    routeNavView.style.display = 'none';
    locationListView.style.display = 'flex';
  }

  window.appClearRoute = clearRoute;

  window.appOnLanguageChange = function(lang) {
    renderLocationList();
    populateRoutePickers();
    renderGeoJSONLayer();
    if (currentDestLoc) recalculateLiveRoute();
  };

  window.openBuildingInfoModal = function(loc) {
    if (!loc) return;
    const modal = document.getElementById('bldg-info-modal');
    const titleEl = document.getElementById('bldg-modal-title');
    const catEl = document.getElementById('bldg-modal-category');
    const navBtn = document.getElementById('bldg-modal-nav-btn');
    const floorBtn = document.getElementById('bldg-modal-floor-btn');
    const startBtn = document.getElementById('bldg-modal-start-btn');

    if (titleEl) titleEl.textContent = loc.translations[i18n.currentLang] || loc.name;
    if (catEl) catEl.textContent = loc.category || 'Building';

    if (navBtn) {
      navBtn.onclick = function() {
        if (modal) modal.classList.remove('active');
        window.appNavigateTo(loc.id);
      };
    }
    if (floorBtn) {
      floorBtn.onclick = function() {
        if (modal) modal.classList.remove('active');
        window.appOpenFloorPlan(loc.id);
      };
    }
    if (startBtn) {
      startBtn.onclick = function() {
        if (modal) modal.classList.remove('active');
        window.appSetStartLocation(loc.id);
      };
    }

    if (modal) modal.classList.add('active');
  };

  window.appSetStartLocation = function(locId) {
    const loc = campusData.locations.find(l => l.id === locId);
    if (loc) {
      currentStartLoc = loc;
      const originSelect = document.getElementById('origin-select');
      if (originSelect) originSelect.value = loc.id;
      map.closePopup();
      if (currentDestLoc) recalculateLiveRoute();
    }
  };

  // 6. Setup Global Language Selectors Everywhere
  function setupLanguage() {
    document.querySelectorAll('#lang-select, .lang-selector, .lang-select-global').forEach(select => {
      select.addEventListener('change', (e) => {
        const lang = e.target.value;
        i18n.setLanguage(lang);
        renderLocationList();
        populateRoutePickers();
        if (currentDestLoc) recalculateLiveRoute();
      });
    });
  }

  // 7. Check URL Parameters for QR Code Scan Entry
  function checkURLParams() {
    const urlParams = new URLSearchParams(window.location.search);
    const fromParam = urlParams.get('from') || urlParams.get('start');
    const destParam = urlParams.get('dest');

    if (fromParam) {
      const gateLoc = campusData.locations.find(l => l.id.toLowerCase() === fromParam.toLowerCase() || l.name.toLowerCase().includes(fromParam.toLowerCase()));
      if (gateLoc) {
        currentStartLoc = gateLoc;
        const originSelect = document.getElementById('origin-select');
        if (originSelect) originSelect.value = gateLoc.id;

        qrBanner.style.display = 'flex';
        qrBannerText.textContent = i18n.t('scannedBanner', { gate: gateLoc.translations[i18n.currentLang] || gateLoc.name });
        CMS.logScan(gateLoc.id, destParam);
      }
    }

    if (destParam) {
      const destLoc = campusData.locations.find(l => l.id.toLowerCase() === destParam.toLowerCase());
      if (destLoc) {
        setTimeout(() => startRouteNavigation(destLoc), 500);
      }
    }
  }

  // 8. Render Location List in Drawer
  function renderLocationList() {
    if (!campusData || !campusData.locations) {
      locationListView.innerHTML = `<p style="color:var(--text-muted); font-size:0.85rem; text-align:center; padding:20px;"><i class="fa-solid fa-circle-notch fa-spin"></i> Loading locations...</p>`;
      return;
    }

    const query = searchInput.value.toLowerCase().trim();
    locationListView.innerHTML = '';

    const filtered = campusData.locations.filter(loc => {
      const nameMatch = loc.name.toLowerCase().includes(query) || (loc.translations[i18n.currentLang] && loc.translations[i18n.currentLang].toLowerCase().includes(query));
      const catMatch = activeCategory === 'all' || loc.category === activeCategory;
      return nameMatch && catMatch;
    });

    if (filtered.length === 0) {
      locationListView.innerHTML = `<p style="color:var(--text-muted); font-size:0.85rem; text-align:center; padding:20px;">No locations found matching your search.</p>`;
      return;
    }

    filtered.forEach(loc => {
      const transName = loc.translations[i18n.currentLang] || loc.name;
      const card = document.createElement('div');
      card.className = 'location-card';
      card.innerHTML = `
        <div class="location-info">
          <h4>${transName}</h4>
          <p><i class="fa-solid fa-door-open" style="color:var(--primary-500); font-size:0.7rem;"></i> Entrance Door &bull; ${loc.category}</p>
        </div>
        <div style="display:flex; gap:6px; align-items:center;">
          <span class="location-badge" onclick="event.stopPropagation(); window.appNavigateTo('${loc.id}')"><i class="fa-solid fa-diamond-turn-right"></i> Go</span>
        </div>
      `;
      card.addEventListener('click', () => {
        map.flyTo([loc.lat, loc.lon], 19, { duration: 1 });
        window.openBuildingInfoModal(loc);
      });
      locationListView.appendChild(card);
    });
  }

  // 9. Indoor Floor Plan Modal
  window.appOpenFloorPlan = function(locId) {
    const loc = campusData.locations.find(l => l.id === locId);
    if (!loc) return;

    modalBuildingTitle.textContent = loc.translations[i18n.currentLang] || loc.name;
    floorTabsContainer.innerHTML = '';
    indoorRoomsGrid.innerHTML = '';

    const floors = loc.floors || [];
    floors.forEach((f, idx) => {
      const btn = document.createElement('button');
      btn.className = `floor-btn ${idx === 0 ? 'active' : ''}`;
      btn.textContent = f.label;
      btn.addEventListener('click', () => {
        document.querySelectorAll('.floor-btn').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        renderRooms(f.rooms);
      });
      floorTabsContainer.appendChild(btn);
    });

    if (floors.length > 0) {
      renderRooms(floors[0].rooms);
    }

    floorModal.classList.add('active');
  };

  function renderRooms(rooms) {
    indoorRoomsGrid.innerHTML = '';
    rooms.forEach(r => {
      const box = document.createElement('div');
      box.className = 'room-box';
      box.innerHTML = `
        <span class="room-code">${r.code}</span>
        <span class="room-title">${r.title}</span>
        <span style="font-size:0.7rem; color:var(--accent-emerald); font-weight:600;"><i class="fa-solid fa-circle-check"></i> ${r.accessible ? 'Wheelchair Accessible' : 'Standard'}</span>
      `;
      indoorRoomsGrid.appendChild(box);
    });
  }

  window.appNavigateTo = function(locId) {
    const loc = campusData.locations.find(l => l.id === locId);
    if (loc) {
      map.closePopup();
      startRouteNavigation(loc);
    }
  };

  // 10. Event Listeners Setup
  function setupEventListeners() {
    searchInput.addEventListener('input', () => {
      clearSearchBtn.style.display = searchInput.value ? 'block' : 'none';
      renderLocationList();
    });

    clearSearchBtn.addEventListener('click', () => {
      searchInput.value = '';
      clearSearchBtn.style.display = 'none';
      renderLocationList();
    });

    categoryChips.addEventListener('click', (e) => {
      if (e.target.classList.contains('chip')) {
        document.querySelectorAll('.category-chips .chip').forEach(c => c.classList.remove('active'));
        e.target.classList.add('active');
        activeCategory = e.target.getAttribute('data-category');
        renderLocationList();
      }
    });

    clearRouteBtn.addEventListener('click', clearRoute);
    accessibleToggle.addEventListener('change', () => {
      if (currentDestLoc) recalculateLiveRoute();
    });

    document.getElementById('close-banner-btn').addEventListener('click', () => {
      qrBanner.style.display = 'none';
    });

    closeFloorModalBtn.addEventListener('click', () => {
      floorModal.classList.remove('active');
    });

    openAdminBtn.addEventListener('click', () => {
      adminModal.classList.add('active');
    });

    closeAdminModalBtn.addEventListener('click', () => {
      adminModal.classList.remove('active');
    });

    adminLoginBtn.addEventListener('click', () => {
      if (adminPasscode.value === 'admin123' || adminPasscode.value === '') {
        document.getElementById('admin-login-section').style.display = 'none';
        document.getElementById('admin-dashboard-section').style.display = 'block';
        setupAdminDashboard();
      } else {
        alert('Invalid Passcode! Try admin123');
      }
    });
  }

  function setupAdminDashboard() {
    const annSelect = document.getElementById('ann-location-select');
    const qrSelect = document.getElementById('qr-gate-select');
    
    annSelect.innerHTML = `<option value="all">All Campus Locations</option>`;
    qrSelect.innerHTML = '';

    campusData.locations.forEach(loc => {
      annSelect.innerHTML += `<option value="${loc.id}">${loc.name}</option>`;
      qrSelect.innerHTML += `<option value="${loc.id}">${loc.name}</option>`;
    });

    qrSelect.addEventListener('change', updateQRCode);

    document.getElementById('announcement-form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const locId = annSelect.value;
      const title = document.getElementById('ann-title').value;
      const msg = document.getElementById('ann-message').value;
      const type = document.getElementById('ann-type').value;

      const success = await CMS.createAnnouncement(locId, title, msg, type);
      if (success) {
        alert('Announcement Published Successfully!');
        document.getElementById('ann-title').value = '';
        document.getElementById('ann-message').value = '';
      }
    });
  }

  function updateQRCode() {
    const gateId = document.getElementById('qr-gate-select').value;
    const targetUrl = CMS.generateQRURL(gateId);
    const qrImg = document.getElementById('qr-image-display');
    const urlDisplay = document.getElementById('qr-target-url');

    qrImg.src = `https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=${encodeURIComponent(targetUrl)}`;
    urlDisplay.textContent = targetUrl;
  }

  // Start application
  init();
});
